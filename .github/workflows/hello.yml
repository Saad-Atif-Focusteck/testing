name: Hello World

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

jobs:
  hello:
    runs-on: windows-latest

    steps:
      - name: Hello step
        run: echo "Hello World"
